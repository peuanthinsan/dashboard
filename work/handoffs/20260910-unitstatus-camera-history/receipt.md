# Implementation and verification receipt

Root: Codex; surface model/effort UNKNOWN. Root owns edits and release. Codex read-only model/storage reviewers found and verified fixes for out-of-order omission, friendly-name aliases, persistence recovery and fleet identity. Claude external planning was blocked by automatic approval review. The user then approved sending the prepared patch to Claude, including follow-up review of fixes, and the first review completed using claude-fable-5 (requested effort high). Final review of the expanded 13-file camera-history and scrollbar payload was rejected by automatic approval review because authorization for that complete follow-up payload was not recognized. A precise approval/waiver question is pending. No fallback circumvented either rejection.

Implemented persistent, per-camera PostgreSQL observation union and an authenticated server snapshot endpoint. Source/company/all-fleet authorization precedes reads and writes; returned primary/CH rows are scoped. Source namespace also includes authorized fleet IDs. Vehicle keys use source-reported fleet or exact scoped username membership, vehicle number and optional device ID, not optional CH-derived display labels. Different configured fleet scopes learn independently.

Fresh later recording omission is Offline; stale/missing/future/malformed or older/equal evidence is Unknown. Explicit configuration/status precedence, Intercom Online, BIGTH checklist and Reverse BSD/geofence behavior are retained. Learned inventory alone cannot confirm complete installation or geofence. History-store failures are disclosed and in-session knowledge survives recovery. Learning occurs on dashboard refreshes; no unattended polling or upstream backfill is claimed.

Verification:

- Original 89 model tests retained and passing; 37 additional model/route/namespace tests passing (126 total across targeted files). The final authoritative-mask refinement was then verified by rerunning the affected 25-test camera-history file.
- Targeted ESLint, TypeScript and whitespace checks pass.
- Actual PostgreSQL temporary-table proof passed: union across snapshots, older/repeated observations, reload/read, source/vehicle isolation, monotonic first/last timestamps. Temporary table was dropped; no persistent production schema/data changed by the proof.
- Isolated rendered fixture: initial 1/2/3 Online, later 2/3 retains camera 1 Offline, camera 4 blank, adjacent details explain historical omission, reload retains the same result. Meaningful page/controls, no framework overlay, no relevant console errors. Screenshot remains private in `/private/tmp/unitstatus-camera-history-20260910/browser-loss.png`.
- Fresh Vercel production configuration for the verified linked project was corroborated by all three known dashboard IDs. Cached local production configuration differs and is not used. New history table is absent before migration. Credentials remain in private temporary storage and are excluded from review/commit.
- Mouse QA with 300 synthetic vehicles confirmed both visible 16px native scrollbar tracks, horizontal thumb dragging, vertical wheel scrolling and vertical thumb dragging. Column headings and vehicle names remain sticky. At a 390px viewport, the 1667px table stays inside a 342px scroll region; document width is 384px. A read-only Codex CSS review found no actionable issues. Screenshot remains private in `/private/tmp/unitstatus-camera-history-20260910/scrollbars-300.png`.

First Claude review adjudication: fixed the masked-camera out-of-order omission case, fresh explicit video loss with blank recording, legacy named-tab gid validation, and CH-fetch failure degradation to only source-proven authorized rows. The conditional installation-panel finding does not apply: the current shared UI has no buildInstallRows calls, and legacy dashboards do not use this endpoint. Verified endpoint access checks against the actual dashboard page gate. Final frozen source patch SHA-256: `b1c63c6c7b34b7f3ee7c583e5efc32401ad6d1abb53a370e760fd46ce1c946c8`.

Release status: not yet committed or deployed. The prepared additive SQL migration must run on the verified current production target before deploying dependent code. No environment settings, database targets, source sheets or upstream Spark files change.
