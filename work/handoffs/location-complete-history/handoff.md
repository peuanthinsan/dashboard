# Complete selected-vehicle history

User objective: selecting one vehicle loads all its records automatically; remove the redundant “1 vehicles · 1 drivers” location-records subtitle. Preserve the single-select vehicle dropdown and vehicle/fleet isolation. Existing permission to push fixes to GitHub persists; merge/deployment is not authorized.

Base: 7aabe34313adb7dfee93df3c8096d04e18be777d on origin/main. Isolated worktree /private/tmp/songdee-location-complete-history, branch codex/location-complete-history. Codex root owns source writer lease and git. Model/effort UNKNOWN (surface not verified). Delegated Claude planner/reviewer is read-only.

Evidence: current hook loads 2,000 records then requires manual Load older history. Its 25,000 ceiling can silently stop native date history. Text timestamp workaround repeatedly fetches and sorts all selected rows, then slices each page. Messer has 6 vehicles with 4,288–11,034 records each. User explicitly wants all selected-vehicle records. Network responses must stay bounded to avoid serverless response-size failures.

Executable plan: add location-history mode that paginates only selected vehicle/fleet records in bounded 2,000-row responses in sheet order with no total cap; retain legacy location-rows behavior for already-open clients. Auto-fetch all pages in the client until exhausted, cancel on selection/refresh/unmount. Expose/cache only complete history; loading progress can show count but incomplete KPI/export values must never appear complete. Use existing normalization to preserve chronology, including Updated Time fallback. Remove Load older history controls and loaded-history/cap copy. Remove redundant KPI subtitle. Preserve other filters and table pagination. Do not prefetch other vehicles or change metrics definitions beyond full-history input coverage.

Allowed paths: app/dashboards/locationVehicleFetch.ts and .test.ts; app/dashboards/locationVehicleData.ts; app/dashboards/locationVehicleHistory.ts and .test.ts (new); app/dashboards/locationDataV1.ts and .test.ts (fallback chronology only); app/dashboards/useLocationVehicleData.ts; app/dashboards/LocationDataV1Dashboard.tsx; app/api/sheets/[sheetId]/[gid]/route.ts; e2e/location-vehicle-filter.spec.ts; docs/METRICS.md; this handoff directory. No .env*, database, production configuration, shared date parser, unrelated templates, git mutation by delegates, or external writes.

Ownership: Codex backend worker owns locationVehicleFetch.ts/.test.ts and sheet API route; root owns all other listed paths. The read-only Codex design audit supports this plan and identified Updated Time ordering to preserve. Claude Fable planning invocation was blocked by automatic approval review because external code disclosure was not explicitly authorized. The user then explicitly waived Claude review (“no need to review with claude just fo it”); no Claude transmission occurred. Independent Codex review applies instead.

Acceptance: all 11,034 records for Messer’s initial vehicle are represented after loading; no 2,000/25,000 total cap; only the selected vehicle appears; switch during later-page fetch never leaks stale data; partial failures produce retryable error and no false complete totals; malformed/non-progressing pages cannot loop forever. Bounded network payloads, complete-history cache only, chronological results, unchanged table paging, no redundant vehicle/driver subtitle. Targeted unit tests plus focused existing browser spec and actual-sheet read-only proof; no broad local suites. Claude opposite-provider review required or explicitly report unavailability.
